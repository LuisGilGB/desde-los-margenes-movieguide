{
  "name": "desde-los-margenes-movieguide",
  "version": "0.2.2",
  "author": "Luis Gil Gutiérrez de la Barreda",
  "private": true,
  "scripts": {
    "serve": "npm start --prefix server/",
    "server-install": "npm install --prefix server/",
    "watch": "npm start --prefix client/",
    "admin-watch": "npm start --prefix admin-client/",
    "client": "npm run watch",
    "admin-client": "npm run admin-watch",
    "client-install": "npm install --prefix client/",
    "admin-client-install": "npm install --prefix admin-client/",
    "install-all": "npm run server-install && npm run client-install && npm run admin-client-install",
    "prettier:check": "prettier --check .",
    "prettier:write": "prettier --write .",
    "prepare": "husky install"
  },
  "devDependencies": {
    "axios": "^0.21.1",
    "chalk": "^4.1.1",
    "fs-extra": "^9.1.0",
    "husky": "^6.0.0",
    "lint-staged": "^11.0.0",
    "prettier": "^2.2.1"
  },
  "lint-staged": {
    "**/*": "prettier --write --ignore-unknown"
  }
}
