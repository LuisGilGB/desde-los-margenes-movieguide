{
  "name": "desde-los-margenes-movieguide",
  "version": "0.2.0",
  "author": "Luis Gil Gutiérrez de la Barreda",
  "private": true,
  "scripts": {
    "serve": "npm start --prefix server/",
    "server-install": "npm install --prefix server/",
    "watch": "npm start --prefix client/",
    "client": "npm run watch",
    "client-install": "npm install --prefix client/",
    "install-all": "npm run server-install && npm run client-install"
  }
}
